import { css } from '@emotion/react';

export const styles = {
  logo: css({
    mr: 2,
    fontWeight: 'bold',
  }),
  icon: css({
    fontSize: 32,
  }),
  closeBtn: css({
    margin: 2,
    position: 'absolute',
    top: 0,
    right: 0,
  }),
};
