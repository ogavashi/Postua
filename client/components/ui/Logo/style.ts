import { css } from '@emotion/react';

export const styles = {
  logo: css({
    mr: 2,
    fontWeight: 'bold',
    color: 'inherit',
    textDecoration: 'none',
  }),
  icon: css({
    fontSize: 32,
  }),
};
