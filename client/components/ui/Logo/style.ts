import { css } from '@emotion/react';

export const styles = {
  root: css({
    color: 'inherit',
    textDecoration: 'none',
  }),
  logo: css({
    mr: 2,
    fontWeight: 'bold',
    textDecoration: 'none',
  }),
  icon: css({
    fontSize: 32,
  }),
};
